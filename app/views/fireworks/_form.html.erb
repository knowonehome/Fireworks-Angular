<%= form_for(firework) do |f| %>
  <% if firework.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(firework.errors.count, "error") %> prohibited this firework from being saved:</h2>

      <ul>
      <% firework.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :style %>
    <%= f.text_field :style %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>
  Alternative Names: <br>
  <%= f.fields_for :alternate_names do |an| %>
    <%= an.label :name %>
    <%= an.text_field :name  %>
    <br>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
